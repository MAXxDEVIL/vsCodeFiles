## Arithmetic Operator

### Basic Opprators

        // Sum
         //z = x + y;
        
        // Subtraction
         // z = x - y;

        // Multiplication
         // z = x * y;

        // Divition
         // z = x / y;

        // Mod (reminder)
         // z = x % y;

---

### Augmented Assignment Operators
        
        // x = x + y;

        // Sum
         //x += y;
        
        // Subtraction
         // x -= y;

        // Multiplication 
         // x *= y;

        // Divition
         // x /= y;

        // Mod (reminder)
         //x %= y;