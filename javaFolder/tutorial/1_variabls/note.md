## Variables

> Variable = A reusable containor foe a value.
             A variable behaves as if it was the value it contains.

## Type of Variabls

🍎 > Primitive = Simple value stored directly in memory (like "stack").

🍏 > Nonprimitive = It stored memory address of auther variables.

     🍎 Primitive    vs   🍏 Nonprimitive
    --------------------------------------
    Int(int)          |    String (String)
                      |
    Double (double)   |    Array (array)
                      |
    Char (char)       |    Object (object)
                      |
    Boolean (boolean) |
                      |
    Float (float)     |
                      |



